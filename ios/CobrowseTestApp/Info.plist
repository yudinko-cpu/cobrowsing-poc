<?xml version="1.0" encoding="UTF-8"?>
<!--
  Ключи, которые нужно добавить в Info.plist проекта CobrowseTestApp.
  В Xcode-проекте, созданном через File → New → Project, Info.plist генерируется
  автоматически. Нужно либо править сгенерированный, либо добавлять ключи
  через "Target → Info" вкладку.

  Все три ключа обязательны для dev-сборки:
    1. NSAppTransportSecurity — разрешает ws:// на localhost
    2. NSMicrophoneUsageDescription — LiveKit публикует audio track
    3. NSCameraUsageDescription — на будущее (P2 mobile camera share)
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>

    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>

    <key>LSRequiresIPhoneOS</key>
    <true/>

    <key>UILaunchScreen</key>
    <dict/>

    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>

    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsLocalNetworking</key>
        <true/>
    </dict>

    <key>NSMicrophoneUsageDescription</key>
    <string>Для голосовой связи с оператором поддержки</string>

    <key>NSCameraUsageDescription</key>
    <string>Опционально: показать камеру оператору при диагностике устройства</string>

</dict>
</plist>
